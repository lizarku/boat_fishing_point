# 🎣 Fishing Points

보트낚시 포인트 관리 레포지토리 (Lowrance HOOK-4 어탐기 웨이포인트)

## 📁 디렉토리 구조

```
fishing_points/
├── gpx/                    # 원본 GPX 파일
│   ├── chungnam_points.gpx    # 충남 (당진, 서천, 보령, 태안 등)
│   ├── gangwon_points.gpx     # 강원 (강릉, 양양, 고성, 속초 등)
│   ├── gyeongsang_points.gpx  # 경상 (울산, 포항, 영덕 등)
│   ├── jeju_points.gpx        # 제주
│   ├── jeolla_points.gpx      # 전라 (군산, 목포, 여수 등)
│   └── my_own_points.gpx      # 직접 찍은 포인트
├── data/                   # 변환/분석 데이터
│   ├── all_points.csv         # 전체 포인트 (CSV)
│   ├── all_points.json        # 전체 포인트 (JSON)
│   ├── duplicates.csv         # 중복 포인트 목록
│   └── {region}_points.csv    # 지역별 CSV
└── scripts/                # 관리 스크립트
    └── gpx_utils.py           # GPX 유틸리티
```

## 📊 현재 데이터 현황

| 지역 | 포인트 수 | 비고 |
|------|-----------|------|
| 충남 | 1,068 | 당진, 서천, 보령, 태안, 안면도 |
| 강원 | 578 | 강릉, 양양, 고성, 속초, 주문진 |
| 전라 | 535 | 군산, 목포, 여수, 신안 |
| 경상 | 461 | 울산, 포항, 영덕, 거제 |
| 제주 | 68 | 제주 전역 |
| 내 포인트 | 41 | 직접 어탐으로 찍은 좌표 |
| **합계** | **2,751** | |

### 좌표 범위
- 위도: 33.08° ~ 38.58° (제주 ~ 고성)
- 경도: 119.76° ~ 129.59°

## 🔧 사용법

### 1. 중복 검사 실행
```bash
cd scripts
python gpx_utils.py --gpx-dir ../gpx --output-dir ../data --distance 10
```

**옵션:**
- `--distance`: 중복 판정 거리 (기본 10m)
- `--no-duplicates`: 중복 검사 건너뛰기

### 2. CSV/JSON 데이터 활용

```python
import pandas as pd

# 전체 포인트 로드
df = pd.read_csv('data/all_points.csv')

# 충남 지역만 필터링
chungnam = df[df['source_file'] == 'chungnam_points.gpx']

# 특정 좌표 근처 포인트 검색
target_lat, target_lon = 36.82, 126.15
nearby = df[
    (abs(df['lat'] - target_lat) < 0.1) & 
    (abs(df['lon'] - target_lon) < 0.1)
]
```

### 3. GPX 파일 직접 사용

Lowrance 어탐기에 직접 로드하거나, GPX 호환 앱에서 사용:
- Lowrance: SD카드로 복사
- 네이버 지도: GPX 가져오기
- Google Earth: GPX 열기

## 📝 데이터 필드

| 필드 | 설명 | 예시 |
|------|------|------|
| lat | 위도 | 36.812667 |
| lon | 경도 | 126.140397 |
| name | 포인트 이름 | 당진_장고항_사각_1973 |
| description | 상세 설명 | 당진시 석문면 장고항 - 사각 (1973년) |
| comment | 추가 정보 | 수심:31.1m |
| time | 기록 시간 | 2025-11-15T14:27:30Z |
| symbol | 심볼 | Fish, Waypoint |
| source_file | 출처 파일 | chungnam_points.gpx |

## ⚠️ 중복 데이터

현재 861건의 중복이 발견되었습니다 (10m 이내).

주요 원인:
1. 지역 경계 중복 (서천군 - 충남/전라 양쪽에 존재)
2. 동일 위치 다른 시설물 (연도가 다른 인공어초)

중복 목록: `data/duplicates.csv`

## 🔄 데이터 갱신

어탐기에서 새 포인트 추출 후:
1. `gpx/my_own_points.gpx` 업데이트
2. 스크립트 실행하여 CSV/JSON 재생성
3. 중복 검사 결과 확인

## 📱 출조 시 활용

1. **사전 조사**: `all_points.csv`에서 해당 지역 필터링
2. **어탐기 로드**: 필요한 GPX만 SD카드에 복사
3. **현장 기록**: 새 포인트는 어탐기에서 직접 저장
4. **귀항 후**: GPX 추출하여 `my_own_points.gpx`에 병합

---

*Last updated: 2025-12-14*
"# boat_fishing_point" 
